function cleanResponse(raw) {    if (!raw) return "";    return raw        .replace(/```json/gi, "")        .replace(/```/g, "")        .replace(/\r/g, "")        .replace(/\/\/.*$/gm, "")        .trim();}/** * Extract only the first valid JSON object using a stack-based approach. */function extractObject(raw) {    let startIndex = -1;    let braceCount = 0;    for (let i = 0; i < raw.length; i++) {        if (raw[i] === '{') {            if (braceCount === 0) {                startIndex = i;            }            braceCount++;        } else if (raw[i] === '}') {            braceCount--;            if (braceCount === 0 && startIndex !== -1) {                return raw.slice(startIndex, i + 1);            }        }    }    return null;}/** * Repairs common invalid JSON generated by LLMs. */function repairJson(json) {    return json        // Remove latex escapes        .replace(/\\\(/g, "(")        .replace(/\\\)/g, ")")        // Remove trailing commas        .replace(/,\s*}/g, "}")        .replace(/,\s*]/g, "]")        // Remove control characters        .replace(/[\u0000-\u001F]+/g, " ")        // Normalize quotes        .replace(/[“”]/g, '"')        .replace(/[‘’]/g, "'");}/** * Safely parse model output. */export function parseModelResponse(raw, promptName = "Unknown") {    try {        const cleaned = cleanResponse(raw);        const extracted = extractObject(cleaned);        if (!extracted) {            console.log(`\n===== RAW RESPONSE (${promptName}) =====`);            console.log(cleaned);            console.log("========================");            throw new Error(`No JSON object found for ${promptName}.`);        }        try {            return JSON.parse(extracted);        } catch (parseErr) {            const repaired = repairJson(extracted);            return JSON.parse(repaired);        }    }    catch (err) {        console.log(`\n❌ JSON Parse Error in ${promptName}`);        console.log(err.message);        return null;    }}